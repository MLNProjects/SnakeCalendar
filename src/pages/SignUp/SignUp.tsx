import * as React from "react";

// State is never set so we use the '{}' type.
export class SignUp extends React.Component<{}, {}> {
  public render() {
    return (
      <div id="Nope" style={{"backgroundColor": "yellow", "height": "200px"}}>
        </div>
    );
  }
}
